/**
 * 1.visit: console.firebase.google.com
 * 2.create project (skip google analytics)
 * 3.Register app (create config)
 * 4.install firebase: npm install firebase 
 * 5.add config file to your project
 * 6.Danger: Do not publish of make firebase config to public by pushing those to github 
 * 7.go to docs > Build > Authentication > web > get started .
 * 8.export app from the firebase.config.js file: export default app 
 * 9.Login.jsx: import getAuth from firebase /auth 
 * 10.create const auth = getAuth (app)
 * 11.import googleAuthprovider and create a new provider 
 * 12.use singInWithpopup and pass auth and provider
 * 13.Activate sing-in method (google, facebook, github ,etc.)
 * 14.[vite]: change 127.0.0.1 to localhost 
 * 
 * 
 * More auth provider
 * 1.activate the auth provider (ceate app, provide redirect url, client id, client secret)

* 
 * */ 